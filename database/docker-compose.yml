services:
  postgres:
    image: postgres:17
    ports:
      - 3002:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: rocktom
    volumes:
      - postgres_data:/Users/armandcolin/rocktom/.data/postgres

volumes:
  postgres_data: